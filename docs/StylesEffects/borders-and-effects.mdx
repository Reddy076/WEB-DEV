---
id: borders-and-effects
title: Borders and Effects
sidebar_label: Borders and Effects
sidebar_position: 9
---

# Borders and Effects

## 1. Border Radius
Softens the look of the web.
- `border-radius: 5px`: Small round.
- `border-radius: 50%`: Circular.

## 2. Box Shadow
Adds depth.
- `box-shadow: 10px 10px 5px grey;`
- Order: X-offset, Y-offset, Blur, Color.
- `inset`: Shadow inside the box.

## 3. Outline
Similar to border, but:
- Does not take up space (overlaps).
- Cannot be circular (in older browsers).
- Often used for Accessibility focus rings.

## 4. Filters
Photoshop-like effects in the browser.
- `blur(5px)`
- `grayscale(100%)`
- `brightness(150%)`
- `drop-shadow()` (Better than box-shadow for PNGs).

import Playground from '@site/src/components/Playground';

## 5. Interactive Playground

Contrast `box-shadow` (box based) with `filter: drop-shadow` (shape based). The SVG star has a drop shadow that follows its shape!

<Playground 
  initialHTML={`<div class="card">Box Shadow</div>\n<br>\n<img src="https://docusaurus.io/img/docusaurus_keytar.svg" class="star" alt="shape">`}
  initialCSS={`.card {\n  background: white;\n  padding: 20px;\n  box-shadow: 5px 5px 10px #888;\n  display: inline-block;\n}\n\n.star {\n  width: 100px;\n  /* Follows image shape */\n  filter: drop-shadow(5px 5px 5px red);\n}`}
/>
