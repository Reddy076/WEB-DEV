---
sidebar_position: 1
---

import HTMLPlayground from '@site/src/components/HTMLPlayground';

# Overview of HTML

**HTML (HyperText Markup Language)** is the standard markup language for creating web pages and web applications.

### Key Points:
- **Markup Language**: Uses tags to structure content
- **Not a Programming Language**: Defines structure, not logic
- **Foundation of Web**: Every website uses HTML
- **Works with CSS and JavaScript**: HTML (structure), CSS (style), JavaScript (behavior)

### Interactive Example

Try modifying the HTML below to see the result instantly:

<HTMLPlayground initialCode={`<!DOCTYPE html>
<html>
<head>
<style>
  body { font-family: sans-serif; }
  h1 { color: #0066cc; }
</style>
</head>
<body>

<h1>Hello World!</h1>
<p>This is my first <strong>interactive</strong> HTML page.</p>

<button>Click Me</button>

</body>
</html>
`} />

## Basic HTML Structure

Every HTML document follows this structure:

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Page Title</title>
</head>
<body>
    <h1>Hello World!</h1>
    <p>This is a paragraph.</p>
</body>
</html>
```
